#include "splinch.dtsi"

/ {
    chosen {
        zmk,kscan = &mock_kscan;
    };

    mock_kscan: kscan {
        compatible = "zmk,kscan-mock";
        columns = <14>; 
        rows = <5>;
        events = <0>;
    };
};